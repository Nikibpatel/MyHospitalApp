<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.hopsital_project">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".medicine.Medicine"/>

        <activity android:name=".hospital_module.H_Profile"/>

        <activity android:name=".hospital_module.h_login">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".hospital_module.h_dashboard" >

        </activity>
        <activity android:name=".hospital_module.h_reg" />
        <activity android:name=".h_doctor_dashboard" />
        <activity android:name=".h_donor_dashboard" />
        <activity android:name=".h_nurse_dashboard" />
        <activity android:name=".h_patient_dashboard" />
        <activity android:name=".h_chemist_dashboard" />
        <activity android:name=".h_profile_dashboard"></activity>
    </application>

</manifest>